# CI/CD

Planned: Jenkins lint and security checks running in parallel, followed by a
Docker build and Docker Hub push. An Azure DevOps equivalent is a bonus task.

Status: not implemented. Jenkins and Docker Hub access are pending.
