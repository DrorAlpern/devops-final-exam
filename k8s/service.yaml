apiVersion: v1
kind: Service
metadata:
  name: flask-aws-monitor
  namespace: devops-monitor
spec:
  type: ClusterIP
  selector:
    app: flask-aws-monitor
  ports:
    - name: http
      port: 5001
      targetPort: http
