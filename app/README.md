# Application

Planned: the course Flask AWS monitor, Python dependencies, a multi-stage
Dockerfile, and container run instructions. Preserve the initial bug in the
Docker feature checkpoint before the debugging section fixes it.

Status: not implemented. No application run has been verified yet.
